(((a)->b))->(f->((p->(q)))) //Вывод: формула не выводима
p->(p->p)) //Вывод: формула не корректна
mn->(p->p) //Вывод: формула не корректна
q->(pp) //Вывод: формула не корректна
->a->b->c //Вывод: формула не корректна
a->b->c-> //Вывод: формула не корректна

(s->(r->q))->((s->r)->(s->q)) //Вывод: формула (s->(r->q))->((s->r)->(s->q)) выводима из аксиомы S путем замены переменной "p" на выражение "r"
(p->(r->q))->((p->r)->(p->q)) //Вывод: формула (p->(r->q))->((p->r)->(p->q)) выводима из формулы (s->(r->q))->((s->r)->(s->q)) путем замены переменной "s" на выражение "p"
(p->(r->p))->((p->r)->(p->p)) //Вывод: формула (p->(r->p))->((p->r)->(p->p)) выводима из формулы (s->(r->q))->((s->r)->(s->q)) путем замены переменной "q" на выражение "p"
(p->(r->p)) //Вывод: формула (p->(r->p)) выводима из аксиомы K путем замены переменной "q" на выражение "r"
p->r->(p->p) //Вывод: формула p->r->(p->p) выводима из формул (p->(r->p))->((p->r)->(p->p)) и (p->(r->p)) по правилу modus ponens
(p->(q->p))->(p->p) //Вывод: формула (p->(q->p))->(p->p) выводима из формулы p->r->(p->p) путем замены переменной "r" на выражение "q->p"
(p->p) //Вывод: (p->p) выводима из формулы (p->(q->p))->(p->p) и аксиомы K по правилу modus ponens
